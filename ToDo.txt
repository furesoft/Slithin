- render rm file to pdf

- tools:
	rss crawler

- macrosystem: man kann ein tool in ein makro verwandeln mit vordefinierten eigentschaften - wird als neues tool angezeigt, führt ursprüngliches tool mit den definierten eigenschaften auf

- syncqueue, aufhebende elemente beide löschen:
	add something
	remove something

	-> nichts tun

- add ability to tools to be executed in contextmenu on notebookspage, if a predicate is true
	- pdf notebooks only appear menuitem if selected notebook is pdf
	- if contextmenu click, give metadata as argument
		- on tool: if metadata is given, select file automaticly in modals