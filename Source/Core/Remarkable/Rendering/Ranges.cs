using System;

namespace Slithin.Core.Remarkable.Rendering
{
    public struct Ranges
    {
        public const float MaxDirection = (float)(Math.PI * 2.0);
        public const float MaxPressure = 1.0f;
        public const float MinDirection = 0.0f;
        public const float MinPressure = 0.0f;
    }
}
